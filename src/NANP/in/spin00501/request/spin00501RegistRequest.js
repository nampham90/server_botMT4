const AbstractRequest = require("../../../../common/abstract/AbstractRequest");

const Joi = require('joi');
class Spin00501RegistRequest extends AbstractRequest {
    constructor(req) {
        super(req)
    }
}

module.exports = Spin00501RegistRequest;